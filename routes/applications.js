const router=require('express').Router(); const {auth,roles,premium}=require('../middleware/auth'); const {read,transact,id,now}=require('../db'); const {send}=require('../services/email');
router.post('/',auth,roles('seeker'),premium,async(req,res,next)=>{try{const listing=read().listings.find(x=>x.id===req.body?.listingId&&x.status==='approved');if(!listing)return res.status(404).json({success:false,message:'Listing not found',error:'NOT_FOUND'});if(read().applications.some(a=>a.listingId===listing.id&&a.seekerId===req.user.id))return res.status(409).json({success:false,message:'Already applied',error:'DUPLICATE_APPLICATION'});const a={id:id(),listingId:listing.id,seekerId:req.user.id,employerId:listing.employerId,coverLetter:String(req.body?.coverLetter||'').slice(0,5000),status:'pending',createdAt:now(),updatedAt:now()};await transact(db=>db.applications.push(a));const e=read().users.find(u=>u.id===listing.employerId);if(e)send(e.email,'New application on Ishla',`${req.user.firstName} applied for ${listing.title}.`);res.status(201).json({success:true,data:a})}catch(e){next(e)}});
router.get('/my',auth,roles('seeker'),(req,res)=>res.json({success:true,data:read().applications.filter(x=>x.seekerId===req.user.id)})); router.get('/employer',auth,roles('employer'),(req,res)=>res.json({success:true,data:read().applications.filter(x=>x.employerId===req.user.id)});
router.put('/:id/status',auth,roles('employer'),async(req,res,next)=>{try{const a=read().applications.find(x=>x.id===req.params.id&&x.employerId===req.user.id);if(!a)return res.status(404).json({success:false,message:'Application not found',error:'NOT_FOUND'});if(!['pending','reviewing','accepted','rejected','withdrawn'].includes(req.body?.status))return res.status(422).json({success:false,message:'Invalid status',error:'VALIDATION_ERROR'});await transact(db=>{const x=db.applications.find(v=>v.id===a.id);x.status=req.body.status;x.updatedAt=now()});res.json({success:true,data:read().applications.find(x=>x.id===a.id)})}catch(e){next(e)}});
router.post('/saved',auth,async(req,res,next)=>{try{const listing=read().listings.find(x=>x.id===req.body?.listingId&&x.status==='approved');if(!listing)return res.status(404).json({success:false,message:'Listing not found',error:'NOT_FOUND'});if(read().savedListings.some(x=>x.userId===req.user.id&&x.listingId===listing.id))return res.json({success:true,data:{saved:true}});const s={id:id(),userId:req.user.id,listingId:listing.id,createdAt:now(),updatedAt:now()};await transact(db=>db.savedListings.push(s));res.status(201).json({success:true,data:s})}catch(e){next(e)}});
router.delete('/saved/:listingId',auth,async(req,res,next)=>{try{await transact(db=>{db.savedListings=db.savedListings.filter(x=>!(x.userId===req.user.id&&x.listingId===req.params.listingId))});res.json({success:true,data:{saved:false}})}catch(e){next(e)}});router.get('/saved',auth,(req,res)=>{const db=read();res.json({success:true,data:db.savedListings.filter(x=>x.userId===req.user.id).map(x=>db.listings.find(l=>l.id===x.listingId)).filter(Boolean)})});
router.post('/offers',auth,roles('employer'),async(req,res,next)=>{try{const seeker=read().users.find(x=>x.id===req.body?.seekerId&&x.role==='seeker');if(!seeker)return res.status(404).json({success:false,message:'Seeker not found',error:'NOT_FOUND'});const o={id:id(),employerId:req.user.id,seekerId:seeker.id,listingId:req.body?.listingId||null,message:String(req.body?.message||'').slice(0,5000),status:'pending',createdAt:now(),updatedAt:now()};await transact(db=>db.offers.push(o));send(seeker.email,'New job offer on Ishla',o.message);res.status(201).json({success:true,data:o})}catch(e){next(e)}});
router.put('/offers/:id',auth,roles('seeker'),async(req,res,next)=>{try{const o=read().offers.find(x=>x.id===req.params.id&&x.seekerId===req.user.id);if(!o)return res.status(404).json({success:false,message:'Offer not found',error:'NOT_FOUND'});if(!['accepted','rejected'].includes(req.body?.status))return res.status(422).json({success:false,message:'Invalid status',error:'VALIDATION_ERROR'});await transact(db=>{const x=db.offers.find(v=>v.id===o.id);x.status=req.body.status;x.updatedAt=now()});res.json({success:true,data:read().offers.find(x=>x.id===o.id)})}catch(e){next(e)}}); module.exports=router;
