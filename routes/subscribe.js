const router=require('express').Router();const crypto=require('node:crypto');const {auth}=require('../middleware/auth');const {read,transact,id,now}=require('../db');const {paymentAmount}=require('../services/payment');router.post('/',auth,async(req,res,next)=>{try{const provider=req.body?.provider;if(!['click','payme'].includes(provider))return res.status(422).json({success:false,message:'Unsupported payment provider',error:'VALIDATION_ERROR'});const p={id:id(),userId:req.user.id,provider,merchantTransactionId:`ISHLA-${Date.now()}-${crypto.randomBytes(4).toString('hex')}`,amount:paymentAmount(),currency:'UZS',status:'pending',type:'premium_monthly',createdAt:now(),updatedAt:now(),completedAt:null,metadata:{}};await transact(db=>db.payments.push(p));if(provider==='click')return res.status(201).json({success:true,data:{paymentId:p.id,checkoutUrl:`https://my.click.uz/services/pay?service_id=${encodeURIComponent(process.env.CLICK_SERVICE_ID||'')}&merchant_id=${encodeURIComponent(process.env.CLICK_MERCHANT_ID||'')}&amount=${p.amount}&transaction_param=${encodeURIComponent(p.merchantTransactionId)}`}});const payload=Buffer.from(JSON.stringify({m:'Ishla Premium',ac:{order_id:p.merchantTransactionId},a:p.amount*100,c:'UZS'})).toString('base64url');res.status(201).json({success:true,data:{paymentId:p.id,checkoutUrl:`https://checkout.paycom.uz/${payload}`}})}catch(e){next(e)}});module.exports=router;
