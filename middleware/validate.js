function validate(req,res,next){if(!req.body||typeof req.body!=='object') return res.status(400).json({success:false,message:'Invalid JSON body',error:'INVALID_BODY'}); next()} function email(v){return String(v||'').trim().toLowerCase()} module.exports={validate,email};
