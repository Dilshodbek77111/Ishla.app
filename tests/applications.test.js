const test=require('node:test');const assert=require('node:assert/strict');const request=require('supertest');process.env.JWT_SECRET='test-secret';const app=require('../server');test('applications require authentication',async()=>{const r=await request(app).post('/api/applications').send({listingId:'x'});assert.equal(r.status,401)});test('saved jobs require authentication',async()=>{const r=await request(app).get('/api/applications/saved');assert.equal(r.status,401)});
